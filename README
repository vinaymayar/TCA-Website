-beginnings of an actual page

-template with menu bar, body for text, header with logo and footer for other information

-header uses a picture of my new dual screen setup as a placeholder

-you should use dreamweaver cs5 so we're consistent, since we'll be adding a 
lot of DW template files and things
